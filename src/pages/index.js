import React from "react"
import { Layout, Head, Header, Gallery } from "../components"

const IndexPage = () => (
  <Layout>
    <Head />
    <Header />
    <Gallery />
  </Layout>
)

export default IndexPage
